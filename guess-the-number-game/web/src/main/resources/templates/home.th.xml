<thlogic>
    <attr sel="#homeHeader" th:replace="~{fragments/head::headFragment}"/>
    <attr sel="#welcome" th:text="#{game.welcome}"/>
    <attr sel="#homePlay" th:text="#{game.play}" th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}"
          action="play" method="post"/>
    <attr sel="#homeFooter" th:replace="~{fragments/footer::footerFragment}"/>
</thlogic>